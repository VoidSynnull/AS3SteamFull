package game.data.animation.entity.character.poptropolis 
{
	import game.data.animation.entity.character.Default;

	public class HurdleStop extends Default
	{		
		public function HurdleStop()
		{
			super.characterXmlPath = super.XML_PATH + super.TYPE_HUMAN + "poptropolis/hurdleStop" + ".xml";
		}
	}
}