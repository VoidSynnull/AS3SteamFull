package game.data.animation.entity.character 
{
	public class BottleShake extends Default
	{
		public function BottleShake()
		{
			super.characterXmlPath = super.XML_PATH + super.TYPE_HUMAN + "bottleShake" + ".xml";
		}
	}
}