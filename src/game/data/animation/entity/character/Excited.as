package game.data.animation.entity.character
{
	public class Excited extends Default
	{
		public function Excited()
		{
			// So far only for apes
			super.apeXmlPath = super.XML_PATH + super.TYPE_APE + "navigation/excited" + ".xml";
		}
	}
}