package game.data.animation.entity.character.poptropolis 
{
	import game.data.animation.entity.character.Default;

	public class HurdleRun extends Default
	{		
		public function HurdleRun()
		{
			super.characterXmlPath = super.XML_PATH + super.TYPE_HUMAN + "poptropolis/hurdleRun" + ".xml";
		}
	}
}
