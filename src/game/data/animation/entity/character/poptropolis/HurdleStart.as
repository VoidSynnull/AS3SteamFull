package game.data.animation.entity.character.poptropolis 
{
	import game.data.animation.entity.character.Default;

	public class HurdleStart extends Default
	{		
		public function HurdleStart()
		{
			super.characterXmlPath = super.XML_PATH + super.TYPE_HUMAN + "poptropolis/hurdleStart" + ".xml";
		}
	}
}