package game.data.animation.entity.character.poptropolis 
{
	import game.data.animation.entity.character.Default;

	public class HurdleJump extends Default
	{		
		public function HurdleJump()
		{
			super.characterXmlPath = super.XML_PATH + super.TYPE_HUMAN + "poptropolis/hurdleJump" + ".xml";
		}
	}
}