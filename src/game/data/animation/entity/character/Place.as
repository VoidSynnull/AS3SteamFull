package game.data.animation.entity.character 
{
	import ash.core.Entity;
	/**
	 * ...
	 * @author Bard
	 */
	public class Place extends Default
	{		
		public function Place()
		{
			super.characterXmlPath = super.XML_PATH + super.TYPE_HUMAN + "place" + ".xml";
		}
	}

}