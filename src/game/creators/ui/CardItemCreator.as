package game.creators.ui
{
	

	public class CardItemCreator
	{
		/*
		public function create( xml:XML = null ):Entity
		{
			var entity:Entity = new Entity();
			entity.add( new Display );			// add both dispaly types as the card may switch between the two
			entity.add( new Spatial );
			entity.add( new Motion );
			
			var cardItem:CardItem = new CardItem();
			if( xml != null )
			{
				cardItem.cardData = new CardItemData(xml);
			}
			entity.add( cardItem );

			return entity;
		}	
		*/
	}
}